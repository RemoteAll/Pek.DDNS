[package]
name = "Rust_DDNS"
version = "0.1.0"
edition = "2021"
description = "高性能、可扩展 DDNS（动态域名解析）工具 - Rust 重写版"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["gzip"] }
chrono = { version = "0.4", features = ["clock"] }
